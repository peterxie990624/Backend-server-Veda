// ============================================================
// 模块化配置 - 可随时开关
// 将此文件整体移植到其他项目时，只需修改以下配置
// ============================================================

module.exports = {
  // 服务端口（Nginx 反向代理到此端口）
  PORT: 3001,

  // JWT 密钥（生产环境请替换为随机字符串）
  JWT_SECRET: 'vedabase-memo-secret-2026-change-in-production',

  // JWT 有效期
  JWT_EXPIRES_IN: '30d',

  // 数据库文件路径
  DB_PATH: '/home/ubuntu/memo-api/db/memo.db',

  // ============================================================
  // 功能开关 - 设为 false 可关闭对应模块
  // ============================================================
  FEATURES: {
    // 用户注册功能（false = 禁止新用户注册，已有用户仍可登录）
    ALLOW_REGISTER: true,

    // 备忘录功能
    MEMOS_ENABLED: true,
  },

  // CORS 允许的来源（* 表示允许所有，生产环境建议指定域名）
  CORS_ORIGIN: '*',
};
